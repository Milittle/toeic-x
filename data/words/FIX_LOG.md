# 词库修复记录

> 由 `scripts/apply_word_fixes.py` 追加；改完 Excel 仍需 `npm run words` 重新生成 JSON。
> 每一节都对应一次可回滚的写入，备份文件名在节内列出。

## 2026-09-12 21:04 释义回写（第一轮，3242 条）

- 计划：`llm_fix_plan.json`（LLM 释义审查 4059 个唯一单词 → 3244 条 ok=false → QA 过滤掉 2 条）
- 应用：`set_gloss` 3242 条；命中 3242，未命中 0；覆盖 3 张表 / 4391 个单元格
- 备份：`单词本.bak-20260912-210255.xlsx`（写入前原样）、`单词本.bak-20260912-210437.xlsx`
- 说明：第一次用 openpyxl 保存时丢掉了 `综合分` / `推荐级别` 的公式缓存值（`level`、`compositeScore` 整列变空），已回滚重做。现在写回改为**直接改写 xlsx 包内 sheet XML**，只替换目标单元格，公式与缓存值原样保留。

## 2026-09-12 21:09 补齐缺音标 / 缺英文简释

- 计划：`gap_fix_plan.json`（`scripts/fill_word_gaps.py` 生成；37 个单词 → 30 条音标 + 11 条英文简释）
- 应用：`set_phonetic` 30、`set_endef` 11、`set_note` 37（备注列写明「由 LLM 补全，待人工复核」）；命中 78，未命中 0
- 备份：`单词本.bak-20260912-210908.xlsx`
- 说明：音标按词库现有 ASCII 风格归一化（`aɪ→ai`、`ʊ→u`、`əʊ→әu`、`ˈ→'`）；这批音标/简释是**模型建议值**，尚未人工逐条核对

## 2026-09-12 21:1x 非 ASCII 词头 → id 折叠（代码改动）

- 代码：`scripts/extract_words.py` 新增 `assign_ids()`，`slugify()` 先做 NFKD 折叠
- 结果：`résumé → resume-2`、`café → cafe`、`entrée → entree`（**词头显示不变**，仍是带重音的正确拼写）
- 约束：同一词表内 id 唯一（三张表各自校验通过），同一单词跨词表 id 一致（ADR-0006 的收藏共用前提）
- 影响：`word_notebook` 目前为空，旧 id（`r-sum`/`caf`/`entr-e`）没有收藏数据需要迁移

## 2026-09-12 21:13 补录 25 条搭配（34 行词库标签不再悬空）

- 来源：词库「关联固定搭配」里有 34 行（25 个不同表达）在搭配库中找不到对应条目，页面只能显示成纯文本
- 做法：把 25 条补进 `TOEIC_Reading_固定搭配与惯用法_最终版.xlsx` 的 `高频汇总`（初稿 `data/collocations/word_label_additions.json`，中文/类型人工逐条复核；优先级统一 A；`来源类型=词库关联补录` 以便与教材条目区分）
- 回填：25 条的 `P5/P6/P7次数` 与 `题册原文精确出现次数` 由题库扫描结果回填（4 条在题库中确实没有用例，保持 0）
- 备份：`TOEIC_Reading_固定搭配与惯用法_最终版.bak-20260912-211320.xlsx`
- 结果：搭配库 876 → 901 条；现有条目 id 零变动，5 条搭配收藏全部仍可解析；词库「关联搭配无法解析」34 → 0
- 备注：新增的 `thank A for B`（110 链接）、`access to`（43 链接）进入搭配库自检的「链接虚高」清单，与既有 17 条同属锚点匹配的已知现象

## 2026-09-12 21:16 释义回写（第二轮，18 条残留）

- 计划：`residual_fix_plan.json`（清理第一轮漏下的域标签 / 超长 / 缺词性前缀 / 重复词性）
- 应用：`set_gloss` 18 条；命中 18，未命中 0
- 备份：`单词本.bak-20260912-211636.xlsx`
- 结果：`VERIFICATION.md` 的 C 域标签 25 → 0、D 释义过长 3 → 0、E 无词性前缀 2 → 0、E 词性重复 2 → 0

## 2026-09-12 22:2x 修复后抽样复核（语义层，未闭环的证据）

- 做法：从修好的 4059 个单词里**随机抽 200 个**（seed 20260912），用与审查同一套 prompt 再判一次「释义是否准确完整」，结果存 `postfix_sample_review.json`
- 结果：**通过 181 / 仍建议再改 16 / 调用出错 3 → 通过率 90.5%**
- 对比：修复前全量审查的通过率约 20%（815/4059），说明这一轮修复有效；但**还剩约 9.5% 可以再打磨**，典型是「缺一个托业常用义项」（`budget` 缺形容词义、`land` 缺「降落」、`addition` 缺 `in addition to`）和「还留着生僻义」（`plate` 的「电镀」、`regular` 的「正规军」）
- 含义：结构层已经清零，**语义层没有完全闭环**——要真闭环需要再跑一轮全量修复 + 复核，或接受当前 90.5% 的水平

## 当前词库自检状态（`npm run verify-words`）

| 检查项 | 现状 |
|---|---|
| A 词条碎片/垃圾 | 0 |
| A 非 ASCII 词头 | 6（`résumé`/`café`/`entrée` ×2 词表，拼写合法，id 已折叠） |
| B 同 id 行字段一致性 | 0 不一致（1500 组全部核对） |
| C 释义域标签 | 0 |
| D 释义过长 | 0 |
| E 空释义 / 无词性 / 词性重复 / 截断 / 控制字符 | 全 0 |
| F 缺音标 / 缺英文简释 | 0 / 0 |
| F 词性列全空 | 5559（源表设计如此，`pos` 字段页面不使用） |
| G 词频列无法近似复现 | 659（生成脚本缺失，见下） |
| H 级别/综合分倒序 | ngsl2809 的「基础」是词类不是难度（设计如此） |
| I 关联搭配无法解析 | 0 |

## 待办（尚未处理，需要决定或补脚本）

1. **词频列不可复现**：`题库总次数 / Part5次数 / Part6次数 / Part7次数 / Part5答案次数` 没有生成脚本；`综合分` 与 `推荐级别` 是工作簿公式（已抄录进 `VERIFICATION.md` H 节），输入不可复现则分数只能当估算。要么补脚本重算，要么在页面上标注为估算值。
2. **`ngsl2809` 的「基础」**是功能词词类，不是难度等级；级别筛选跨三个 Tab 不可直接比较，建议页面加说明。
3. **词性列全空**：`词性` 列 5559 行为空，`words.json` 的 `pos` 恒为 null 且页面从不展示——要么补数据，要么从模型和页面里删掉这个字段。
4. **LLM 补的 37 条音标/英文简释待人工抽查**（Excel 备注列已标记）。
