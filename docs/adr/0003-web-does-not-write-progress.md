# web 应用不写 PROGRESS.md，进度真相仍归 PROGRESS.md

PROGRESS.md 是备考计划的唯一进度真相，由 cron 任务按周维护（完成状态、关键指标、验收判定、调整决定）。web 应用是做题数据的唯一记录者（SQLite 中的作答与复测记录），提供每周汇总视图，供人工或 cron 回填 PROGRESS.md，但不直接写该文件。这样避免两个写入方对同一真相文件产生冲突，也保留 cron 现有的证据驱动推进逻辑不变。若未来要自动回填，应先定义单向同步协议。
