# 题库静态 JSON + 动态数据 SQLite 双数据层

题库是书籍内容的数字化，只读且需要可审计：提取质量靠人工抽样校验，JSON 可 diff、可逐题核对，因此以静态 JSON 文件随项目存储（`data/questions/`）。作答历史、成绩、错题、复测调度等动态记录写入本地 SQLite（`data/app.db`）。两种数据的生命周期不同——题库随内容更新而变、只读；记录随使用持续增长、可写——分开存储比单库更简单，也避免了题库被误写的风险。
