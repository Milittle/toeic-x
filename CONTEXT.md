# Tuoye（托业阅读 Web）Context

本项目是托业阅读 Part 5/6/7 的个人局域网练习系统：题库静态只读，作答和复测状态写入本地 SQLite。

## 核心术语

**套题 (Test)**：一套完整的托业阅读模拟，100 题，对应 Test 01–10。

**题组 (Passage set)**：Part 6/7 中共享同一篇文章的一组题目；P7 题组可能对应单篇、双篇或三篇文章。

**练习模式 (Practice mode)**：逐题作答并即时反馈，用于复盘和复测；不产生合法计分样本。

**模拟模式 (Timed test mode)**：75 分钟闭卷计时，提交前不返回答案和解析；成绩作为首次计分样本。

**首次成绩 (First-try score)**：首次闭卷定时完成的成绩。复盘或重做结果不得覆盖它。

**未见题样本 (Unseen-question sample)**：开始前没有看过题目内容的模拟样本。套题只要被练习模式或中断的模拟打开，就会被标记为已见题。

**错因 (Error pattern)**：每道错题的一个主因标签：G 语法知识、V 词汇/搭配、S 句法解析、E 证据定位、T 时间/策略、A 注意/操作、L 听觉加工、K 材料问题。

**复测 (Retest)**：对到期错题在练习模式下重新作答。复测按 D+2、D+7、D+21 排期，结果不影响首次成绩。

**搭配 (Collocation)**：从教材 Vocabulary Check 和解析中整理出的固定表达，只作为静态参照语料，可链接到已经开放的题目。

**单词 (Word)**：词库中的单词实体，带音标、释义、词表归属、题库词频和推荐级别。当前 Web 只提供浏览和收藏，不记录掌握状态或单词级 SRS。

**收藏夹 (Favorites)**：用户主动保存的搭配或单词。收藏与“答错题后的复测”是两套独立状态。

## 数据边界

- `data/questions/`、`data/collocations/`、`data/words/`：静态只读数据；
- `data/app.db`：动态作答、错因、复测和收藏状态；
- `PROGRESS.md`、每日任务和 Hermes Cron 记录：不由 Web 写入，也不属于 Web 运行时状态；
- 题库答案/解析属于受保护内容，模拟模式客户端不得拿到答案字段。

## 不属于当前 Web 的内容

听力 Part 1–4、每日 11:45 微训练、8 周周计划、自动消息投递和多用户能力属于 Hermes 训练目录的职责。若需要修改这些流程，应在 `/root/workspace/cron_tasks/toeic-8-week-plan/` 中处理，不要把它们重新耦合进本 Web 应用。
